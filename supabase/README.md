# Supabase (Phase 1)

Setup is documented in **`docs/SUPABASE_SETUP.md`**: create project, run migration, create Storage buckets `uploads` and `deliverables`, and set env vars.

Migrations: `00001_phase1_schema.sql`, `00002_add_pet_name_to_generations.sql`, `00003_guest_token_usage.sql` (guest token tracking for 2 free portraits).
