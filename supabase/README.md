# Supabase (Phase 1)

Setup is documented in **`docs/SUPABASE_SETUP.md`**: create project, run migration, create Storage buckets `uploads` and `deliverables`, and set env vars.

Migration file: `migrations/00001_phase1_schema.sql`.
